@model IEnumerable<HeritageAllianceApp.Models.Related>

@{
    ViewBag.Title = "Admin";
}

<h2>Admin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeceasedId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeceasedRelativeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Relationship)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeceasedId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeceasedRelativeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Relationship)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelatedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RelatedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RelatedId })
        </td>
    </tr>
}

</table>
