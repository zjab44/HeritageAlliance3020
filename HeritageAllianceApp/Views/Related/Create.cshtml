@model HeritageAllianceApp.Models.Related

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Related</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DeceasedId, "Deceased")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DeceasedId", String.Empty)
            @Html.ValidationMessageFor(model => model.DeceasedId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DeceasedRelativeId, "DeceasedRelative")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DeceasedRelativeId", String.Empty)
            @Html.ValidationMessageFor(model => model.DeceasedRelativeId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Relationship)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Relationship)
            @Html.ValidationMessageFor(model => model.Relationship)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Admin")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
